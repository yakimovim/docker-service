@using Service.Model
@inherits ResponseSpecificationComponentBase

<Card Title="Probable delay">
	<EditForm class="m-3" Model=@Item>
		<div class="mb-3">
			<label for="probability" class="form-label">Probability (from 0 to 1):</label>
			<InputNumber class="form-control" 
					id="probability" name="probability"
					required min="0" max="1"
					@bind-Value="Item.Probability" />
		</div>
		<div class="mb-3">
			<label for="delay" class="form-label">Delay in seconds:</label>
			<InputNumber class="form-control"
					id="delay" name="delay"
					required min="0"
					@bind-Value="Item.DelayDurationInSeconds" />
		</div>
		<DefinitionButtons Component=@this />
	</EditForm>
</Card>

@code {
	[Parameter]
	public ProbableDelay Item { get; set; }
}
